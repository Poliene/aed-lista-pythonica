def hello_world() -> str: # essa função não recebe nada
    """
    Retorna a string padrão de saudação.

    Returns:
        str: "Hello, World!"
    """
    return "Hello, World!" # ela só precisa devolver a mensagem padrão, então usa o return
